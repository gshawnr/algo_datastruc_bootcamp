/*
  Complete the function to take an input array of strings (transactions) and ouput an ordered array that groups transactions and orders them first by the number of transactions
  secondly by the transaction

  ie 
  input: ["logoff", "text", "logon", "vim", "logon", "logoff"]
  output: ["logoff 2", "logon 2", "text 1", "vim 1"]

*/
function groupTransactions(transactions) {}

module.exports = groupTransactions;
